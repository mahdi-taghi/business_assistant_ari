{
  "name": "ChatSession",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Session title derived from first message"
    },
    "first_message_preview": {
      "type": "string",
      "description": "Preview of the first user message"
    },
    "message_count": {
      "type": "number",
      "default": 0,
      "description": "Total number of messages in session"
    },
    "last_activity": {
      "type": "string",
      "format": "date-time",
      "description": "Last message timestamp"
    }
  },
  "required": [
    "title"
  ]
}